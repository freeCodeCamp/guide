webpackJsonp([0xaef0c94eb95f],{3590:function(n,s){n.exports={data:{markdownRemark:{html:'<h1>Reactive Extensions in Angular</h1>\n<h4>Motivation</h4>\n<p>Reactive extensions for JavaScript (RxJS) is a library for <em>observable</em> data streams. RxJS installs with Angular upon command-line execution of <code>ng new [name-of-application]</code>. This uses the Angular command-line interface (CLI). RxJS supplements how data streamlines through an <code>Observable</code>. The <code>Observable</code> object facilitates the flow of <em>iterable</em> data.</p>\n<p>Streams of data are not the primary use-case. After all, data streams parallel event streams. Events emit so an application knows when data arrives. While event streams form the core of what RxJS supplements, this article refers to them as data streams too.</p>\n<p>Streams execute either synchronously (immediate) or asynchronously (overtime). RxJS handles both cases with ease via the <code>Observable</code> data stream. Strict asynchronicity is toggleable though. Working with in-memory, <em>iterable</em> data happens immediately while external data fetching takes time. Angular supports the RxJS library so that it may handle both use-cases with data streams.</p>\n<h4>Reactive Programming</h4>\n<p>Before diving in, its important to understand the paradigm backing the RxJS library. As mentioned, it works through the <code>Observable</code> object which streamlines event-emitting data.</p>\n<p>RxJS functions around the base <code>Observable</code>. Its whole library supplements what it can do. RxJS even includes other objects including <code>Subject</code>, <code>Subscription</code>, and <code>Observer</code>. Each is its own custom variant of the base <code>Observable</code>.</p>\n<p>RxJS arose from the Reactive Programming paradigm. This paradigm introduced the <em>observable</em> pattern. In it there exists this key idea: a single <code>Observable</code> emits while all its <code>Observer</code>s get notified. <code>Observer</code>s <em>subscribe</em> to <code>Observable</code>s so that they receive notification. This notification can mean several things.</p>\n<p>It might indicate data change or data arrival as commonly stated in this article. It could signal a change in some part of the application that affects the <code>Observer</code>s.</p>\n<p>This <em>observable</em> pattern also strives to decouple concepts. An <code>Observable</code> should be able function without any <code>Observer</code>s and vice versa. This usually means they can stand-alone rather than fully function without each other.</p>\n<p>If curious, you can learn more about the foundations of Reactive Programming by reading <a href="https://en.wikipedia.org/wiki/Reactive_programming">this Wikipedia article</a>. This section covers what is necessary for the rest of the article.</p>\n<h4>Observables</h4>\n<p>To quickly reiterate, <code>Observable</code>s can be <em>observed</em>. This so that one <code>Observable</code> provides feedback to its dependencies based on a stream of data. In RxJS, the <code>Observable</code> is its own factory function used to create <code>Observable</code> objects. Their basic blueprint is as follows.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span> Observable <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'rxjs\'</span><span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> observable <span class="token operator">=</span> Observable<span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token punctuation">(</span>source<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>\n  source<span class="token punctuation">.</span><span class="token function">next</span><span class="token punctuation">(</span>data<span class="token punctuation">)</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n</code></pre>\n      </div>\n<p><code>.next</code> passes data while emitting an event to its observers. An <code>Observable</code> emits data from inside its <code>.create</code> callback using <code>.next</code>. It accepts one argument representing the data to emit. The <code>Observable</code> has not been implemented into JavaScript yet. RxJS provides an replacement from its library.</p>\n<p>The next step is the observers. To tell a function or object to <em>observe</em> an <code>Observable</code>, the following syntax is used: <code>observable.subscribe(observer)</code>. Another way to look at it is <code>producer.subscribe(consumer)</code>. Observables <em>produce</em> data by calling <code>.next</code>. Consumers are then notified while receiving the data.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span> Observable <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'rxjs\'</span><span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> observable <span class="token operator">=</span> Observable<span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token punctuation">(</span>source<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>\n  source<span class="token punctuation">.</span><span class="token function">next</span><span class="token punctuation">(</span><span class="token string">"Hello"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n  source<span class="token punctuation">.</span><span class="token function">next</span><span class="token punctuation">(</span><span class="token string">"World!"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n\nobservable<span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span><span class="token punctuation">(</span>word<span class="token punctuation">)</span> <span class="token operator">=></span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>word<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n<span class="token comment" spellcheck="true">// console output</span>\n<span class="token comment" spellcheck="true">/*\nHello\nWorld!\n*/</span>\n</code></pre>\n      </div>\n<p>Two invocations of <code>.next</code> occur from within the <code>Observable</code>’s <code>.create</code> callback (data producer). This results in two separate console outputs from the observer (data consumer).</p>\n<p>The two invocations of <code>.next</code> represent a synchronous stream of data. Streams conceptualize data as a linear, in-order flow. It either resolves synchronously or asynchronously depending on the data’s availability.</p>\n<p>If data comprising a stream is readily available, it executes synchronously. Otherwise, the stream resolves asynchronously overtime. The order of the data in either case is always the same depending on the invocation of <code>.next</code> within the observable.</p>\n<p>An <code>Observable</code> operates like a queue. Calling <code>.next</code> on a piece of data pushes it to the back of the queue. Data pops from the front once resolved.</p>\n<p><code>Observable</code> data streams hold huge appeal. They are deterministic in their order and execute sensibly depending on data availability. Plus, any number of observers can <em>observe</em> the data source <code>Observable</code>. This means data can produce once and emit anywhere all in one operation.</p>\n<p>Callback functions are not the only way to consume data. Observables can chain into each other as producers and consumers.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token keyword">const</span> observableI <span class="token operator">=</span> Observable<span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token punctuation">(</span>source<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>\n  source<span class="token punctuation">.</span><span class="token function">next</span><span class="token punctuation">(</span><span class="token string">"Hello World!"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> observableII <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Observable</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span><span class="token punctuation">(</span>v<span class="token punctuation">)</span> <span class="token operator">=></span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>v<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n\nobservableI<span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span>observableII<span class="token punctuation">)</span><span class="token punctuation">;</span>\n<span class="token comment" spellcheck="true">// console output</span>\n<span class="token comment" spellcheck="true">/*\nHello World!\n*/</span>\n</code></pre>\n      </div>\n<p><code>.subscribe</code> is on the <code>Observable</code> object. You can call it with an <code>Observable</code> as its source (producer) and another observable as it argument (consumer). Data can flow (emit) through any number of observables.</p>\n<h4>Reactive Extensions for JavaScript (RxJS)</h4>\n<p>Streaming data is nice, but what is the point if observables cannot edit the stream? This where the RxJS library comes into play. It provides operators that perform various mutations on the data stream.</p>\n<p>Angular leverages these operators to transform incoming data. Developers can trim off any unnecessary data from an incoming stream using RxJS operators. This saves memory and alleviates the need for additional transformation logic.</p>\n<p>RxJS offers deviations from the standard <code>Observable</code> like <code>Subject</code>, <code>Subscription</code>, and <code>Observer</code>. Think of these deviations as special flavors of the traditional <code>Observable</code>. They are not necessary to make use of the library. That said, variants like <code>Subject</code> have incredible use-cases that surpass the standard <code>Observable</code>.</p>\n<p>This article sticks with the standard <code>Observable</code>. All the data stream operators from RxJS work through the <code>Observable</code>.</p>\n<p>Many core RxJS operators originated from JavaScript’s Array Extras. The Array object’s prototype contains many parallels to the RxJS library. These are otherwise known as ‘Extras’. Arrays are stream-like structures similar to observable data steams.</p>\n<p>To better understand RxJS operators, this article will briefly address JavaScript’s Array Extras. Each on functions almost identically to its RxJS counterpart. The difference is simply the format of the data (iterable array vs iterable stream).</p>\n<h4>Array Extras</h4>\n<p>Arrays contain lots of utility methods. These methods are called Array Extras. They all exist on the Array object’s prototype. The list below contains five Extras with RxJS parallels.</p>\n<ul>\n<li><code>.reduce</code></li>\n<li><code>.filter</code></li>\n<li><code>.map</code></li>\n<li><code>.every</code></li>\n<li><code>.forEach</code></li>\n</ul>\n<p>For each example, the array iterates over itself to produce a final result.</p>\n<p><code>.reduce</code> minimizes an array into a single value. <code>.filter</code> trims an array with boolean evaluation. <code>.map</code> transforms an array element-by-element. <code>.every</code> evaluates true or false for the entire array depending on a boolean condition. <code>.forEach</code> iterates through an array’s elements.</p>\n<p>Arrays model streams. The are in-order of each other and iterate one-by-one. Observables streamline data elements to their observers in a similar fashion. This is why RxJS includes a logical counterpart to each Array Extra in its library. Granted, RxJS provides way more of its own operators than there are Array Extras.</p>\n<h4>Basic RxJS Operators</h4>\n<p>There is literally an entire library’s worth of RxJS operators. This article focuses on those listed below which the Array Extras inspired.</p>\n<ul>\n<li><code>.reduce</code></li>\n<li><code>.filter</code></li>\n<li><code>.map</code></li>\n<li><code>.every</code></li>\n<li><code>.forEach</code></li>\n</ul>\n<p>Nothing has changed from previous list. Your understanding of Array Extras applies to RxJS operators. The only catch to this is a function called <code>.pipe</code> which will see much use in the next few examples. <code>.pipe</code> chains RxJS operators together. Results from the previous operator segue into the next up until the final operator. The resulting data then emits from the observable stream.</p>\n<p>Note the standard example below. Use it for comparison for each operator’s impact on the emitted data stream.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span> Observable<span class="token punctuation">,</span> <span class="token keyword">from</span> <span class="token punctuation">}</span> <span class="token keyword">from</span> ‘rxjs’<span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> stream<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> observable<span class="token punctuation">:</span> Observable<span class="token operator">&lt;</span><span class="token keyword">number</span><span class="token operator">></span> <span class="token operator">=</span> <span class="token keyword">from</span><span class="token punctuation">(</span>stream<span class="token punctuation">)</span><span class="token punctuation">;</span>\nobservable<span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span><span class="token punctuation">(</span>val<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">)</span> <span class="token operator">=></span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>val<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n<span class="token comment" spellcheck="true">// console output</span>\n<span class="token comment" spellcheck="true">/*\n1\n2\n3\n4\n5\n*/</span>\n</code></pre>\n      </div>\n<p><code>.from</code> converts an array into an <code>Observable</code> object that calls <code>.next</code> on each array element. The <code>.pipe</code> function accepts any number of arguments as array operators. This is where every operator gets implemented. Operators execute on streamlined data in order of their implementation as arguments for <code>.pipe</code>.</p>\n<h5>Reduce</h5>\n<p><code>.reduce</code> minimizes the data stream into a single value before emitting.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span> reduce <span class="token punctuation">}</span> <span class="token keyword">from</span> ‘rxjs<span class="token operator">/</span>operators’<span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> stream<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> observable<span class="token punctuation">:</span> Observable<span class="token operator">&lt;</span><span class="token keyword">number</span><span class="token operator">></span> <span class="token operator">=</span> <span class="token keyword">from</span><span class="token punctuation">(</span>stream<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">pipe</span><span class="token punctuation">(</span>\n  <span class="token function">reduce</span><span class="token punctuation">(</span><span class="token punctuation">(</span>accum<span class="token punctuation">,</span> val<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">(</span>accum <span class="token operator">+</span> val<span class="token punctuation">)</span><span class="token punctuation">)</span>\n<span class="token punctuation">)</span><span class="token punctuation">;</span>\nobservable<span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span><span class="token punctuation">(</span>val<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">)</span> <span class="token operator">=></span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>val<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n<span class="token comment" spellcheck="true">// console output</span>\n<span class="token comment" spellcheck="true">/*\n15\n*/</span>\n</code></pre>\n      </div>\n<h5>Filter</h5>\n<p><code>.filter</code> trims a stream, eliminating stream values that do not satisfy its condition.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span> filter <span class="token punctuation">}</span> <span class="token keyword">from</span> ‘rxjs<span class="token operator">/</span>operators’<span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> stream<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> observable<span class="token punctuation">:</span> Observable<span class="token operator">&lt;</span><span class="token keyword">number</span><span class="token operator">></span> <span class="token operator">=</span> <span class="token keyword">from</span><span class="token punctuation">(</span>stream<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">pipe</span><span class="token punctuation">(</span>\n  <span class="token function">filter</span><span class="token punctuation">(</span><span class="token punctuation">(</span>val<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">(</span>val <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">===</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token comment" spellcheck="true">// filters out odd numbers</span>\n<span class="token punctuation">)</span><span class="token punctuation">;</span>\nobservable<span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span><span class="token punctuation">(</span>val<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">)</span> <span class="token operator">=></span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>val<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n<span class="token comment" spellcheck="true">// console output</span>\n<span class="token comment" spellcheck="true">/*\n2\n4\n*/</span>\n</code></pre>\n      </div>\n<h5>Map</h5>\n<p><code>.map</code> targets and transforms each ongoing stream value.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token keyword">const</span> stream<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n\n<span class="token keyword">const</span> observable<span class="token punctuation">:</span> Observable<span class="token operator">&lt;</span><span class="token keyword">number</span><span class="token operator">></span> <span class="token operator">=</span> <span class="token keyword">from</span><span class="token punctuation">(</span>stream<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">pipe</span><span class="token punctuation">(</span>\n    <span class="token function">map</span><span class="token punctuation">(</span><span class="token punctuation">(</span>val<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">(</span>val <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span>\n<span class="token punctuation">)</span><span class="token punctuation">;</span>\nobservable<span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span><span class="token punctuation">(</span>val<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">)</span> <span class="token operator">=></span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>val<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n<span class="token comment" spellcheck="true">// console output</span>\n<span class="token comment" spellcheck="true">/*\n2\n3\n4\n5\n6\n*/</span>\n</code></pre>\n      </div>\n<h5>Challenge: Every and ForEach</h5>\n<p>With knowledge of <code>.every</code> and <code>.forEach</code> as Array Extras, try to implement them as RxJS operators. Feel free to use the previous examples for guidance. A little bit of practice goes a long way after lots of reading!</p>\n<h4>HTTP in Angular</h4>\n<p>This section brings RxJS and Angular together to show how they interact. Typically, a service provided by Angular will provide the <code>Observable</code>. The <code>Observable</code>’s data stream can then mutate using RxJS operators with <code>.pipe</code>.</p>\n<p>Angular provides an <code>HttpClient</code> service through <code>@angular/common/http</code>. <code>HttpClientModule</code> is also from <code>@angular/common/http</code> and exports the <code>HttpClient</code> service. The application’s root module must import <code>HttpClientModule</code>. This makes <code>HttpClientModule</code> <em>injectable</em> from anywhere in the app.</p>\n<p>The <code>HttpClientModule</code> service makes HTTP requests. These requests are asynchronous. What makes them interesting for Angular is how the request is handled. An <code>Observable</code> gets returned with each request. RxJS can take it away from there.</p>\n<p>The upcoming example uses a public API built by <a href="https://jsonplaceholder.typicode.com">Typicode</a>. The API provides an array of 100 elements per asynchronous <code>GET</code> request.</p>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token comment" spellcheck="true">// ./models/post.model.ts</span>\n\n<span class="token keyword">export</span> <span class="token keyword">interface</span> <span class="token class-name">Post</span> <span class="token punctuation">{</span>\n  userId<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">;</span>\n  id<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">;</span>\n  title<span class="token punctuation">:</span> <span class="token keyword">string</span><span class="token punctuation">;</span>\n  body<span class="token punctuation">:</span> <span class="token keyword">string</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span>\n</code></pre>\n      </div>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token comment" spellcheck="true">// ./services/json.service.ts</span>\n\n<span class="token keyword">import</span> <span class="token punctuation">{</span> HttpClient <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'@angular/common/http\'</span><span class="token punctuation">;</span>\n<span class="token keyword">import</span> <span class="token punctuation">{</span> Injectable <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'@angular/core\'</span><span class="token punctuation">;</span>\n\n<span class="token keyword">import</span> <span class="token punctuation">{</span> Observable<span class="token punctuation">,</span> <span class="token keyword">from</span> <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'rxjs\'</span><span class="token punctuation">;</span>\n<span class="token keyword">import</span> <span class="token punctuation">{</span> switchMap<span class="token punctuation">,</span> map<span class="token punctuation">,</span> filter<span class="token punctuation">,</span> reduce <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'rxjs/operators\'</span><span class="token punctuation">;</span>\n\n<span class="token keyword">import</span> <span class="token punctuation">{</span> Post <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'../models/post.model\'</span><span class="token punctuation">;</span>\n\n@<span class="token function">Injectable</span><span class="token punctuation">(</span><span class="token punctuation">{</span>\n  providedIn<span class="token punctuation">:</span> <span class="token string">\'root\'</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span>\n<span class="token keyword">export</span> <span class="token keyword">class</span> <span class="token class-name">JsonService</span> <span class="token punctuation">{</span>\n  <span class="token keyword">constructor</span><span class="token punctuation">(</span><span class="token keyword">private</span> http<span class="token punctuation">:</span> HttpClient<span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token punctuation">}</span>\n\n  <span class="token function">getPostsByUserId</span><span class="token punctuation">(</span>id<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">)</span><span class="token punctuation">:</span> Observable<span class="token operator">&lt;</span><span class="token keyword">any</span><span class="token operator">></span> <span class="token punctuation">{</span>\n    <span class="token keyword">const</span> trim$ <span class="token operator">=</span> <span class="token punctuation">(</span>stream<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token keyword">from</span><span class="token punctuation">(</span>stream<span class="token punctuation">)</span>\n      <span class="token punctuation">.</span><span class="token function">pipe</span><span class="token punctuation">(</span>\n        <span class="token function">filter</span><span class="token punctuation">(</span><span class="token punctuation">(</span>post<span class="token punctuation">:</span> Post<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token operator">+</span>post<span class="token punctuation">.</span>userId <span class="token operator">===</span> <span class="token operator">+</span>id<span class="token punctuation">)</span><span class="token punctuation">,</span>\n        <span class="token function">map</span><span class="token punctuation">(</span><span class="token punctuation">(</span>post<span class="token punctuation">:</span> Post<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">(</span><span class="token punctuation">{</span> title<span class="token punctuation">:</span> post<span class="token punctuation">.</span>title<span class="token punctuation">,</span> body<span class="token punctuation">:</span> post<span class="token punctuation">.</span>body <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span>\n        <span class="token function">reduce</span><span class="token punctuation">(</span><span class="token punctuation">(</span>accum<span class="token punctuation">:</span> Post<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span> post<span class="token punctuation">:</span> Post<span class="token punctuation">)</span> <span class="token operator">=></span> accum<span class="token punctuation">.</span><span class="token function">concat</span><span class="token punctuation">(</span><span class="token punctuation">[</span>post<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">)</span>\n      <span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n    <span class="token keyword">return</span> <span class="token keyword">this</span><span class="token punctuation">.</span>http<span class="token punctuation">.</span><span class="token keyword">get</span><span class="token punctuation">(</span><span class="token string">"https://jsonplaceholder.typicode.com/posts"</span><span class="token punctuation">)</span>\n      <span class="token punctuation">.</span><span class="token function">pipe</span><span class="token punctuation">(</span>\n        <span class="token function">switchMap</span><span class="token punctuation">(</span><span class="token punctuation">(</span>value<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token function">trim$</span><span class="token punctuation">(</span>value<span class="token punctuation">)</span><span class="token punctuation">)</span>\n      <span class="token punctuation">)</span><span class="token punctuation">;</span>\n  <span class="token punctuation">}</span>\n<span class="token punctuation">}</span>\n</code></pre>\n      </div>\n<div class="gatsby-highlight">\n      <pre class="language-typescript"><code><span class="token comment" spellcheck="true">// ./components/example/example.component.ts</span>\n\n<span class="token keyword">import</span> <span class="token punctuation">{</span> Component <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'@angular/core\'</span><span class="token punctuation">;</span>\n\n<span class="token keyword">import</span> <span class="token punctuation">{</span> JsonService <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'../../services/json.service\'</span><span class="token punctuation">;</span>\n<span class="token keyword">import</span> <span class="token punctuation">{</span> Post <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">\'../../models/post.model\'</span><span class="token punctuation">;</span>\n\n@<span class="token function">Component</span><span class="token punctuation">(</span><span class="token punctuation">{</span>\n  selector<span class="token punctuation">:</span> <span class="token string">\'app-example\'</span><span class="token punctuation">,</span>\n  template<span class="token punctuation">:</span> <span class="token template-string"><span class="token string">`\n  &lt;h1>Request User Posts&lt;/h1>\n  &lt;span>User: &lt;/span>&lt;input #userInput>\n  &lt;button (click)="requestForPosts(userInput.value)">REQUEST&lt;/button>\n  &lt;hr>\n  &lt;ul>\n    &lt;div *ngIf="userPosts">\n      &lt;div *ngFor="let post of userPosts">\n        &lt;h3>{{ post.title }}&lt;/h3>\n        &lt;p>{{ post.body }}&lt;/p>\n      &lt;/div>\n    &lt;/div>\n    &lt;h3 *ngIf="!userPosts">No posts shown...&lt;/h3>\n  &lt;/ul>\n  `</span></span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span>\n<span class="token keyword">export</span> <span class="token keyword">class</span> <span class="token class-name">ExampleComponent</span> <span class="token punctuation">{</span>\n  userPosts<span class="token punctuation">:</span> Post<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n\n  <span class="token keyword">constructor</span><span class="token punctuation">(</span><span class="token keyword">private</span> json<span class="token punctuation">:</span> JsonService<span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token punctuation">}</span>\n\n  <span class="token function">requestForPosts</span><span class="token punctuation">(</span>id<span class="token punctuation">:</span> <span class="token keyword">number</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token keyword">void</span> <span class="token punctuation">{</span>\n    <span class="token keyword">this</span><span class="token punctuation">.</span>json<span class="token punctuation">.</span><span class="token function">getPostsByUserId</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>\n      <span class="token punctuation">.</span><span class="token function">subscribe</span><span class="token punctuation">(</span><span class="token punctuation">(</span>posts<span class="token punctuation">:</span> Post<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span> <span class="token keyword">this</span><span class="token punctuation">.</span>userPosts <span class="token operator">=</span> posts<span class="token punctuation">.</span>length <span class="token operator">></span> <span class="token number">0</span> <span class="token operator">?</span> posts <span class="token punctuation">:</span> <span class="token keyword">null</span><span class="token punctuation">;</span> <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>\n  <span class="token punctuation">}</span>\n<span class="token punctuation">}</span>\n</code></pre>\n      </div>\n<p><code>json.service.ts</code> creates an <code>Observable</code> on behalf of <code>component.example.ts</code>. The component may subscribe to the returned <code>Observable</code>. Only one value emits by the time the <code>Observable</code> resolves the data stream.</p>\n<p>The request to <code>jsonplaceholder.typicode.com</code> yields a single array of 100 posts. The request via <code>HttpClient</code> yields an <code>Observable</code>. The operator <code>switchMap</code> returns another <code>Observable</code> which overwrites the current stream. The variable <code>trim$</code> stores the <code>Observable</code> as its value. Appending a <code>$</code> to a variable used for storing <code>Observable</code>s is convention.</p>\n<p><code>from</code> converts the array from <code>jsonplaceholder.typicode.com</code> into a 100-value-emitting <code>Observable</code>. RxJS operators then sift through each piece of data in the stream. They remove stream values irrelevant to the request. Data trimming takes place so that stream values stay lean of unnecessary information. The final results join together once again as a single array that emits one to its observers.</p>\n<p>In <code>component.example.ts</code>, the JsonService references the method returning the just-described <code>Observable</code>. This method invokes upon button click in the component template. The input box also in the template supplies the single <code>id</code> argument.</p>\n<p>With the button pressed, JsonService returns an <code>Observable</code> that emits a single array. <code>.subscribe</code> invokes on the returned <code>Observable</code>. The component then sets the value of <code>userPosts</code> equal to the emitted array.</p>\n<p>Angular Change Detection picks up on the change in class data. The template updates and <code>*ngFor</code> ensures each array element from <code>userPosts</code> renders its own template element.</p>\n<h4>Conclusion</h4>\n<p>RxJS provides the core <code>Observable</code> along with its operators. The library installs automatically from the command-line using <code>ng new [name-of-app]</code> (Angular CLI). The RxJS core types and operators download to <code>rxjs</code> and <code>rxjs/operators</code>, respectively.</p>\n<p>Even if you do not use the CLI, services such as <code>HttpClient</code> are still usable. The service returns a <code>Promise</code> instead of an <code>Observable</code> if RxJS is unavailable. The <code>Promise</code> object is native to JavaScript unlike the <code>Observable</code>. This will likely change in the next official JavaScript release.</p>\n<p>That said, take full advantage of RxJS! Any iterable structure can accommodate the <code>Observable</code>. With it, the whole RxJS library becomes usable. Its operators efficiently transform data from a stream into results. Plus, observers can subscribe to results, increasing the overall portability of the data.</p>\n<h2>Sources</h2>\n<ul>\n<li><a href="https://angular.io/guide/rx-library">Angular Team. “The RxJS library”. <em>Google</em>. Accessed 5 June 2018.</a></li>\n<li><a href="https://tutorialedge.net/typescript/angular/angular-socket-io-tutorial">Forbes, Elliot. “Creating a Realtime App with Angular and Socket.io Tutorial”. <em>TutorialEdge.net</em>, 10 Jan. 2017. Accessed 5 June 2018.</a></li>\n<li><a href="https://rxjs-dev.firebaseapp.com">RxJS Team. “RxJS Documentation”. <em>RxJS</em>. Accessed 5 June 2018.</a></li>\n<li><a href="https://javascript.tutorialhorizon.com/2017/03/23/rxjs-subject-vs-observable">Sukale, Ryan. “Difference between Rxjs Subject and Observable”. <em>TutorialHorizon</em>, 23 Mar. 2017. Accessed 5 June 2018.</a></li>\n<li><a href="https://en.wikipedia.org/wiki/Reactive_programming">Wikipedia Community. “Reactive programming”. <em>Wikipedia</em>, 2 June 2018. Accessed 5 June 2018.</a></li>\n</ul>\n<h2>Resources</h2>\n<ul>\n<li><a href="https://angular.io/guide">Angular Documentation</a></li>\n<li><a href="https://github.com/angular/angular">Angular on GitHub</a></li>\n<li><a href="https://cli.angular.io">Angular CLI</a></li>\n<li><a href="https://angular.io/guide/rx-library">RxJS and Angular</a></li>\n<li><a href="https://en.wikipedia.org/wiki/Reactive_programming">Reactive Programming</a></li>\n<li><a href="https://rxjs-dev.firebaseapp.com">Reactive Extensions for JavaScript</a></li>\n</ul>',
fields:{slug:"/angular/reactive-extensions/"},frontmatter:{title:"Reactive Extensions"}}},pathContext:{slug:"/angular/reactive-extensions/"}}}});
//# sourceMappingURL=path---angular-reactive-extensions-5a3fb736845884924ad4.js.map