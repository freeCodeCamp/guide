webpackJsonp([0x17a8329c85462b00],{"./node_modules/json-loader/index.js!./.cache/json/python-list-index-method.json":function(e,t){e.exports={data:{markdownRemark:{html:'<h2>List Index Method</h2>\n<p>Among the many functions which come along with the list data structure the <code>index()</code> returns the the first occurrence/index of the element in the list given as an argument to the function. </p>\n<p>Lists are the most basic Python data structure and stores a list of values in order (in comparison to dictionaries, which order doesn’t matter). We retrieve the items by numerical index.</p>\n<p>Keeping in mind the fact that indexing starts from 0, or the first element is taken to be at the 0 index, let’s have a look at some examples. </p>\n<h4>Example Usage:</h4>\n<div class="gatsby-highlight">\n      <pre class="language-py"><code>numbers = [1, 2, 2, 3, 9, 5, 6, 10]\nwords = ["I", "love", "Python", "I", "love"]\n\nprint(numbers.index(9))\nprint(numbers.index(2))\nprint(words.index("I"))\nprint(words.index("am"))</code></pre>\n      </div>\n<h5>Output:</h5>\n<div class="gatsby-highlight">\n      <pre class="language-py"><code>4\n1\n0\nTraceback (most recent call last):\n  File "<stdin>", line 1, in <module>\nValueError: \'am\' is not in list</code></pre>\n      </div>\n<p>Here the first output is very obvious, but the second and third might seem confusing at first.But remember <code>index()</code> returns the first occurrence of the element and hence in this case <code>1</code> and <code>0</code> are the indices where <code>2</code> and <code>"I"</code> occur first in the lists respectively.</p>\n<p>Also, if an element is not found in the list, a <code>ValueError</code> is returned as in the case of indexing <code>"am"</code> in the <code>words</code> list.</p>\n<h4>Optional arguments:</h4>\n<p>You can also use optional arguments to limit your search to a particular subsequence of the list as illustrated by this example:</p>\n<div class="gatsby-highlight">\n      <pre class="language-py"><code>words = ["I","am", "a", "I", "am", "Pythonista"]\n\nprint(words.index("am",2,5))</code></pre>\n      </div>\n<h5>Output:</h5>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>4</code></pre>\n      </div>\n<p>Here although the element is searched between the indices 2(inclusive) and 5(not inclusive) but the returned index is computed relative to the beginning of the full list rather than the start argument.</p>\n<h4>More Information:</h4>\n<p>The official documentation for <code>index()</code> can be found <a href=\'https://docs.python.org/3.6/tutorial/datastructures.html\' target=\'_blank\' rel=\'nofollow\'>here</a></p>',fields:{slug:"/python/list-index-method/"},frontmatter:{title:"List Index Method"}}},pathContext:{slug:"/python/list-index-method/"}}}});
//# sourceMappingURL=path---python-list-index-method-bd987bb01950ac00c926.js.map