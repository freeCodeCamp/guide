webpackJsonp([0x9fe6542674ed3800],{"./node_modules/json-loader/index.js!./.cache/json/go-a-simple-web-server-in-go.json":function(n,e){n.exports={data:{markdownRemark:{html:'<p>The Go programming language is well-known for having a built-in web server. In this article you will learn how you can easily make your own web server with Go. You won’t need any other packages beside the ones that are already built in!</p>\n<p>First, hop in to your text editor. Then create a file called <code>webserver.go</code> and enter the following code:</p>\n<div class="gatsby-highlight">\n      <pre class="language-go"><code><span class="token keyword">package</span> main\n\n<span class="token keyword">import</span> <span class="token punctuation">(</span>\n  <span class="token string">"net/http"</span>\n  <span class="token string">"io"</span>\n<span class="token punctuation">)</span>\n\n<span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>\n  http<span class="token punctuation">.</span><span class="token function">HandleFunc</span><span class="token punctuation">(</span><span class="token string">"/"</span><span class="token punctuation">,</span> servePage<span class="token punctuation">)</span>\n\thttp<span class="token punctuation">.</span><span class="token function">ListenAndServe</span><span class="token punctuation">(</span><span class="token string">":8080"</span><span class="token punctuation">,</span> <span class="token boolean">nil</span><span class="token punctuation">)</span>\n<span class="token punctuation">}</span>\n\n<span class="token keyword">func</span> <span class="token function">servePage</span><span class="token punctuation">(</span>writer http<span class="token punctuation">.</span>ResponseWriter<span class="token punctuation">,</span> reqest <span class="token operator">*</span>http<span class="token punctuation">.</span>Request<span class="token punctuation">)</span> <span class="token punctuation">{</span>\n  io<span class="token punctuation">.</span><span class="token function">WriteString</span><span class="token punctuation">(</span>writer<span class="token punctuation">,</span> <span class="token string">"Hello world!"</span><span class="token punctuation">)</span>\n<span class="token punctuation">}</span>\n</code></pre>\n      </div>\n<p>Let’s break down the block of code above. We import the <code>net/http</code> package: this package contains the web server itself. Then we also import the <code>io</code> package, we will make use of this later to actually serve something to the client.</p>\n<p>In the <code>main</code> function we do two things. First of all we instruct the server to let the function called <code>servePage</code> handle all incoming traffic to <code>/</code> - in this case it means that it handles requests to <em>any</em> <code>URL</code>. The second thing we do is actually activating the server. We do this using a function named <code>ListenAndServe</code>. This function requires two parameters: the <code>port</code> (as <code>string</code>), in this case it’s <code>8080</code>, and the <code>handler</code> (as <code>Handler</code>) - however the last one isn’t important yet. We will just make it <code>nil</code> and everything will work just fine.</p>\n<p>In the <code>servePage</code> we do just one simple thing, for now. Using the <code>io</code> package and the <code>WriteString</code> function that it contains we can respond to the clients’ request with the text <code>Hello world!</code> (or any other string, of course). You also might have noticed that the <code>servePage</code> function has two arguments: the <code>writer</code> and the <code>request</code>. With the writer you can actually respond to a <code>HTTP</code> request and with the <code>request</code> you may get more information about the request itself.</p>\n<p>Congratulations! You just created your first web server! If you want to test it: just run <code>go run webserver.go</code>, fire up a browser and navigate to <code>http://localhost:8080</code>!</p>',fields:{slug:"/go/a-simple-web-server-in-go/"},frontmatter:{title:"A simple web server in Go"}}},pathContext:{slug:"/go/a-simple-web-server-in-go/"}}}});
//# sourceMappingURL=path---go-a-simple-web-server-in-go-1407dea3e3f3afb71ba2.js.map