webpackJsonp([0xfedb2bae3d797000],{"./node_modules/json-loader/index.js!./.cache/json/python-slicestartstopstep.json":function(e,t){e.exports={data:{markdownRemark:{html:'<p><code>slice(start:stop[:step])</code> is an object usually containing a portion of a sequence. A slice is created using the subscript notation, [] with colons between numbers when several are given, such as in variable_name[1:3:5].</p>\n<h2>Arguments</h2>\n<p>This function can be used to slice tuples, arrays and lists.</p>\n<p>The value of the <code>start</code> parameter (or None if not provided)</p>\n<p>The value of the <code>stop</code> parameter (or last index of sequence)</p>\n<p>The value of the <code>step</code> parameter (or None if not provided). It cannot be 0.</p>\n<p>All three must be of integer type.</p>\n<h2>Return</h2>\n<p>If only <code>stop</code> is provided, it generates portion of sequence from index <code>0</code> till <code>stop</code>.</p>\n<p>If only <code>start</code> is provided, it generates portion of sequence after index <code>start</code> till last element.</p>\n<p>If both <code>start</code> and <code>stop</code> are provided, it generates portion of sequence after index <code>start</code> till <code>stop</code>.</p>\n<p>If all three <code>start</code>, <code>stop</code> and <code>step</code> are provided, it generates portion of sequence after index <code>start</code> till <code>stop</code> with increment of index <code>step</code>.</p>\n<h2>Example</h2>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>a = [1, 2, 3, 4, 5, 6, 7, 8]\nprint(a[:5])    # prints [1, 2, 3, 4, 5]\nprint(a[2:])    # prints [3, 4, 5, 6, 7, 8]\nprint(a[2:5])    # prints [3, 4, 5]\nprint(a[2:7:2])    # prints [3, 5, 7]</code></pre>\n      </div>\n<p>You can index the last index of a sequence by using <code>-1</code>:</p>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>a = [1, 2, 3, 4, 5, 6]\nprint(a[-1])    # prints 6\nprint(a[2:-1])    # prints [3, 4, 5]</code></pre>\n      </div>\n<p>You can flip a sequence by using the <code>[::-1]</code> slice notation:</p>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>a = [1, 2, 3, 4, 5, 6]\nprint(a[::-1])    # prints [6, 5, 4, 3, 2, 1]</code></pre>\n      </div>\n<p><a href=\'https://docs.python.org/3/library/functions.html#slice\' target=\'_blank\' rel=\'nofollow\'>Official Documentation</a>\n<img src="//forum.freecodecamp.com/images/emoji/emoji_one/rocket.png?v=2" title=":rocket:" alt=":rocket:" class="forum-image"> <a href=\'https://repl.it/CT5h\' target=\'_blank\' rel=\'nofollow\'>Run Code</a></p>',fields:{slug:"/python/slicestartstopstep/"},frontmatter:{title:"Python Slicestartstopstep"}}},pathContext:{slug:"/python/slicestartstopstep/"}}}});
//# sourceMappingURL=path---python-slicestartstopstep-b8be6424827a9aac7701.js.map