import React from 'react';

export default class Index extends React.Component {
  render() {
    // react-router should push us on to the article directory
    // this component is here just for completeness
    return (
      <div/>
    );
  }
}
