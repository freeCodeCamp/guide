webpackJsonp([0x89eb3463bc8a3800],{"./node_modules/json-loader/index.js!./.cache/json/javascript-standard-objects-regexp-regexp-prototype-exec.json":function(e,t){e.exports={data:{markdownRemark:{html:'<h2>RegExp prototype exec</h2>\n<p>The <strong><code>exec()</code></strong> method executes a search for a match in a specified string. Returns a result array, or <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/null" title="The value null represents the intentional absence of any object value. It is one of JavaScript&#x27;s primitive values."><code>null</code></a>.</p>\n<p>If you are executing a match simply to find true or false, use the <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/test" title="The test() method executes a search for a match between a regular expression and a specified string. Returns true or false."><code>RegExp.prototype.test()</code></a> method or the <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/search" title="The search() method executes a search for a match between a regular expression and this String object."><code>String.prototype.search()</code></a> method.</p>\n<h3>Syntax</h3>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>regexObj.exec(str)</code></pre>\n      </div>\n<h3>Parameters</h3>\n<p><code>str</code></p>\n<p>The string against which to match the regular expression.</p>\n<h3>Return value</h3>\n<p>If the match succeeds, the <code>exec()</code> method returns an array and updates properties of the regular expression object. The returned array has the matched text as the first item, and then one item for each capturing parenthesis that matched containing the text that was captured.</p>\n<p>If the match fails, the <code>exec()</code> method returns <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/null" title="The value null represents the intentional absence of any object value. It is one of JavaScript&#x27;s primitive values."><code>null</code></a>.</p>\n<h3>Description</h3>\n<p>Consider the following example:</p>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>// Match "quick brown" followed by "jumps", ignoring characters in between\n// Remember "brown" and "jumps"\n// Ignore case\nvar re = /quick\\s(brown).+?(jumps)/ig;\nvar result = re.exec(\'The Quick Brown Fox Jumps Over The Lazy Dog\');</code></pre>\n      </div>\n<p>The following table shows the results for this script:</p>\n<h3>Examples</h3>\n<h4>Finding successive matches</h4>\n<p>If your regular expression uses the ”<code>g</code>” flag, you can use the <code>exec()</code> method multiple times to find successive matches in the same string. When you do so, the search starts at the substring of <code>str</code> specified by the regular expression’s <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/lastIndex" title="The lastIndex is a read/write integer property of regular expression instances that specifies the index at which to start the next match."><code>lastIndex</code></a> property (<a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/test" title="The test() method executes a search for a match between a regular expression and a specified string. Returns true or false."><code>test()</code></a> will also advance the <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/lastIndex" title="The lastIndex is a read/write integer property of regular expression instances that specifies the index at which to start the next match."><code>lastIndex</code></a> property). For example, assume you have this script:</p>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>var myRe = /ab*/g;\nvar str = \'abbcdefabh\';\nvar myArray;\nwhile ((myArray = myRe.exec(str)) !== null) {\n  var msg = \'Found \' + myArray[0] + \'. \';\n  msg += \'Next match starts at \' + myRe.lastIndex;\n  console.log(msg);\n}</code></pre>\n      </div>\n<p>This script displays the following text:</p>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>Found abb. Next match starts at 3\nFound ab. Next match starts at 9</code></pre>\n      </div>\n<p>Note: Do not place the regular expression literal (or <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp" title="The RegExp constructor creates a regular expression object for matching text with a pattern."><code>RegExp</code></a> constructor) within the <code>while</code> condition or it will create an infinite loop if there is a match due to the <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/lastIndex" title="The lastIndex is a read/write integer property of regular expression instances that specifies the index at which to start the next match."><code>lastIndex</code></a> property being reset upon each iteration. Also be sure that the global flag is set or a loop will occur here also.</p>\n<h4>Using <code>exec()</code> with <code>RegExp</code> literals</h4>\n<p>You can also use <code>exec()</code> without creating a <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp" title="The RegExp constructor creates a regular expression object for matching text with a pattern."><code>RegExp</code></a> object:</p>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>var matches = /(hello \\S+)/.exec(\'This is a hello world!\');\nconsole.log(matches[1]);</code></pre>\n      </div>\n<p>This will log a message containing ‘hello world!‘.</p>',fields:{slug:"/javascript/standard-objects/regexp/regexp-prototype-exec/"},frontmatter:{title:"RegExp prototype exec"}}},pathContext:{slug:"/javascript/standard-objects/regexp/regexp-prototype-exec/"}}}});
//# sourceMappingURL=path---javascript-standard-objects-regexp-regexp-prototype-exec-e32135bc9c75ef4e36e6.js.map