webpackJsonp([0x914d623c3e03a000],{"./node_modules/json-loader/index.js!./.cache/json/vim-macros.json":function(e,o){e.exports={data:{markdownRemark:{html:'<h2>Macros</h2>\n<p>Recording macros is a way to make some repetitive tasks automatically in VIM.</p>\n<h3>Recording Macros</h3>\n<p>Macros use one of the VIM registers to be storage, each register is indentify by a letter <code>a</code> to <code>z</code>.</p>\n<p>To start a Macro, in Normal Mode, press:</p>\n<div class="gatsby-highlight">\n      <pre class="language-none"><code>q<letter></code></pre>\n      </div>\n<p>Example: <code>qq</code> starts a macro in the register <code>q</code>, <code>qs</code> starts the macro in the register <code>s</code></p>\n<p>At this point you will see in VIM bottom line <code>recording @q</code>, this means everything you type now will be register in the macro.</p>\n<p>To stop record the macro, press <code>&#x3C;ESC></code> to go back to NORMAL mode, and <code>q</code> to quit the macro.</p>\n<p>To execute the macro you record, press <code>@</code> and the register <code>q</code>.</p>\n<h4>The complete process look like this:</h4>\n<ul>\n<li><code>qq</code> -> start record the macro in register <code>q</code></li>\n<li><code>...</code> -> the serie of commands you want to record</li>\n<li><code>&#x3C;ESC>q</code> -> go back to NORMAL mode and quit the macro record</li>\n<li><code>@q</code> -> execute the macro, starting from the line you current are</li>\n<li><code>@@</code> -> execute the macro again</li>\n</ul>\n<h3>More Info</h3>\n<p>You can find more information about macros in the VIM Tips Wiki:\n<a href="http://vim.wikia.com/wiki/Macros">http://vim.wikia.com/wiki/Macros</a></p>',fields:{slug:"/vim/macros/"},frontmatter:{title:"Macros"}}},pathContext:{slug:"/vim/macros/"}}}});
//# sourceMappingURL=path---vim-macros-7c2cb2e562809628a060.js.map