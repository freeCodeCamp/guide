webpackJsonp([119006406911809e4],{"./node_modules/json-loader/index.js!./.cache/json/c-variables.json":function(n,a){n.exports={data:{markdownRemark:{html:'<h1>Using Variables in C</h1>\n<p>Now you know what your options with data types are. Let’s apply it with a simple example here:</p>\n<div class="gatsby-highlight">\n      <pre class="language-c"><code><span class="token macro property">#<span class="token directive keyword">include</span> <span class="token string">&lt;stdio.h></span></span>\n\n<span class="token keyword">int</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>\n    <span class="token keyword">int</span> my_first_variable <span class="token operator">=</span> <span class="token number">12</span><span class="token punctuation">;</span>\n    <span class="token keyword">double</span> my_second_variable <span class="token operator">=</span> <span class="token number">983.9</span><span class="token punctuation">;</span>\n\n    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"My int is %i\\n"</span><span class="token punctuation">,</span> my_first_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"My double is %f\\n"</span><span class="token punctuation">,</span> my_second_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n    <span class="token keyword">return</span> <span class="token number">0</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span>\n</code></pre>\n      </div>\n<p>There’s a lot of new things to take a look at here! You’ve already seen the <code>#include</code> and <code>int main(void)</code>, so that’s not worth dwelling on. What is new is <code>int my_first_variable = 12;</code>.</p>\n<p>From earlier, you should recall that <code>int</code> allows us to store integer values. After the word <code>int</code> comes <code>my_first_variable</code>. This is a variable- it can store values, and you can change what values are being stored in it. We start with a declaration, where we tell the computer that the initial value of this variable is 12. It’s important to tell the computer that we want a variable to exist before we try to use it. Otherwise, the variable won’t exist, and the compiler won’t know what to do when you try to tell it to use a variable that doesn’t exist.</p>\n<p>The next line is <code>double my_second_variable = 983.9</code>. The similar structure from before should make it clear that you’re telling the computer to create a variable called ‘my<em>second</em>variable’ that can hold <code>double</code> values, and that you want it to be set to 983.9.</p>\n<p>The actual name of the variable isn’t important. It can be whatever you want, as long as it isn’t any of the words that C has reserved for the actual language, and it can only include numbers and letters, never any spaces. By convention, C uses clear variable names that substitute underscores for spaces. The variable could also be camelCase, like this:</p>\n<div class="gatsby-highlight">\n      <pre class="language-c"><code><span class="token keyword">double</span> myFirstVariable <span class="token operator">=</span> <span class="token number">983.9</span>\n</code></pre>\n      </div>\n<p>In fact, it would be this way in other languages. However, in C this is not typically done.</p>\n<p>After the variables are created, we start actually using them:</p>\n<div class="gatsby-highlight">\n      <pre class="language-c"><code>    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"My int is %i\\n"</span><span class="token punctuation">,</span> my_first_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"My double is %f\\n"</span><span class="token punctuation">,</span> my_second_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n</code></pre>\n      </div>\n<p>This is the same printf() that you used earlier, but now it has a few different features. First, notice that there are now two things within the parenthesis: the text to be printed to the screen, and the variable. Also notice the <code>%i</code> and the <code>%f</code>. This is called a <em>format specifier</em>, and is used to specify what format something should be printed in. Whenever printf() comes across one of these, it will try to insert the variable given into that point.</p>\n<p>Because the data types of our variables are represented in a computer in several different ways, there are several different ways for C to display them:</p>\n<table>\n<thead>\n<tr>\n<th>Data Type</th>\n<th>Format Specifier</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>char</td>\n<td>%c, or %hhi to print as a number when signed, %hhu to print as a number when unsigned</td>\n</tr>\n<tr>\n<td>short</td>\n<td>%hi, or %hu when unsigned</td>\n</tr>\n<tr>\n<td>int</td>\n<td>%i, %d can also be used</td>\n</tr>\n<tr>\n<td>long</td>\n<td>%li, or %lu when unsigned</td>\n</tr>\n<tr>\n<td>long long</td>\n<td>%lli, or %llu when unsigned</td>\n</tr>\n<tr>\n<td>float</td>\n<td>%f</td>\n</tr>\n<tr>\n<td>double</td>\n<td>%f</td>\n</tr>\n<tr>\n<td>long double</td>\n<td>%Lf</td>\n</tr>\n</tbody>\n</table>\n<p>In order to print a variable, you must have a format specifier, and then a variable to format. Several format specifiers can be together in the same printf(), as well:</p>\n<div class="gatsby-highlight">\n      <pre class="language-c"><code>    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"%i and %f"</span><span class="token punctuation">,</span> my_first_variable<span class="token punctuation">,</span> my_second_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n</code></pre>\n      </div>\n<p>Now let’s start changing the values within our variables. Here’s the same examples from before, but with a few more lines:</p>\n<div class="gatsby-highlight">\n      <pre class="language-c"><code><span class="token macro property">#<span class="token directive keyword">include</span> <span class="token string">&lt;stdio.h></span></span>\n\n<span class="token keyword">int</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>\n    <span class="token keyword">int</span> my_first_variable <span class="token operator">=</span> <span class="token number">12</span><span class="token punctuation">;</span>\n    <span class="token keyword">double</span> my_second_variable <span class="token operator">=</span> <span class="token number">983.9</span><span class="token punctuation">;</span>\n\n    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"My int is %i\\n"</span><span class="token punctuation">,</span> my_first_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"My double is %f\\n"</span><span class="token punctuation">,</span> my_second_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n    my_second_variable <span class="token operator">=</span> <span class="token operator">-</span><span class="token number">18.2</span> <span class="token operator">+</span> my_first_variable<span class="token punctuation">;</span>\n\n    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"Now my double is %f\\n"</span><span class="token punctuation">,</span> my_second_variable<span class="token punctuation">)</span><span class="token punctuation">;</span>\n\n    <span class="token keyword">return</span> <span class="token number">0</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span>\n</code></pre>\n      </div>\n<p>Now there’s a line that reads <code>my_second_variable = -18.2 + my_first_variable;</code>. This equation assigns a new value to the variable on the left. Whenever a new value is being assigned, the variable that it is being assigned to must always be on the left, and must always be there alone. Your program will find the result of the right hand side, and assign it to the variable on the left. In this case, we’ve added my<em>first</em>variable to -18.2. my<em>first</em>variable is 12, and -18.2 + 12 is 6.2, so my<em>second</em>variable becomes 6.2 after this step. We’ll get more into math in a little bit!</p>\n<h1>Before you go on…</h1>\n<h2>A review</h2>\n<ul>\n<li>Variables need to be created before they are used.</li>\n<li>Variables are created in the following format: <code>datatype variable_name = number</code>.</li>\n<li>Format specifiers allow for variables to be printed.</li>\n<li>The equals sign <code>=</code> allows for values to be assigned to variables.</li>\n</ul>',fields:{slug:"/c/variables/"},frontmatter:{title:"Variables in C"}}},pathContext:{slug:"/c/variables/"}}}});
//# sourceMappingURL=path---c-variables-edab8eceaddbcb1b67e4.js.map